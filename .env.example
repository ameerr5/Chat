# Create a GitHub Gist first (with a file named messages.json containing []), then put its ID here:
GIST_ID=your_gist_id_here
# Create a GitHub Personal Access Token with `gist` scope and add it to Vercel environment variables
GITHUB_TOKEN=ghp_xxx